{
  "name": "@ab/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "echo \"(add build step if deploying)\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(add eslint if desired)\"",
    "format": "echo \"(add prettier if desired)\"",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "generate:beginner": "bun scripts/generate-beginner-sessions.ts",
    "cleanup:test": "bun scripts/cleanup-test-sessions.ts"
  },
  "dependencies": {
    "@ab/contracts": "workspace:*",
    "@ab/utils": "workspace:*",
    "@aws-sdk/client-s3": "^3.952.0",
    "@aws-sdk/s3-request-presigner": "^3.952.0",
    "@clerk/backend": "^2.27.1",
    "@prisma/client": "^5.22.0",
    "@types/fs-extra": "^11.0.4",
    "bcryptjs": "^3.0.3",
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0",
    "fluent-ffmpeg": "^2.1.3",
    "fs-extra": "^11.3.2",
    "hono": "^4.5.5",
    "openai": "^6.15.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0",
    "@types/bun": "^1.3.4",
    "@types/fluent-ffmpeg": "^2.1.28",
    "prisma": "^5.22.0",
    "typescript": "^5.6.3"
  },
  "prisma": {
    "seed": "bun prisma/seed.ts"
  }
}