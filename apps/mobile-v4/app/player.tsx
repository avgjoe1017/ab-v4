import PlayerScreen from '@/features/player/PlayerScreen';

/**
 * Expo Router route for Player
 * Routes to the feature-based PlayerScreen component
 */
export default function PlayerRoute() {
  return <PlayerScreen />;
}
